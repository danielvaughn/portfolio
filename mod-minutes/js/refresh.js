// $(document).ready(function() {

	 // function reloadPageWithHash() {
	   // window.location.replace('timer.html#');
	 // }
	//window.location.href.match(/\#/))

	// if ((window.location.hash) == false ) {
		// reloadPageWithHash();
	// };

// });

// $(document).ready(function() {

  // if("#has-reloaded").length == 0) {
 	// $('body').append("<div id="has-reloaded"></div>")	 
	// location.reload();
  // }

// });	